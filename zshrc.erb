ZSH_THEME="wezm"
plugins=(git common-aliases rails bundler gem npm)

source $ZSH/oh-my-zsh.sh
source <%= ENV['HOME'] %>/.aliases
source <%= ENV['HOME'] %>/.functions

if which rbenv > /dev/null; then eval "$(rbenv init -)"; fi

cd ~/Projects
