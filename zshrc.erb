ZSH_THEME="theunraveler"
plugins=(git common-aliases rails brew bundler gem sublime)

source $ZSH/oh-my-zsh.sh
source <%= ENV['HOME'] %>/.aliases
source <%= ENV['HOME'] %>/.functions

if which rbenv > /dev/null; then eval "$(rbenv init -)"; fi

cd ~/Projects
